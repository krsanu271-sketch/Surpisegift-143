<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A Surprise for Shambhavi</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Georgia', serif;
            background: linear-gradient(135deg, #fff5f5 0%, #ffd1d1 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            overflow: hidden;
        }

        .card {
            background: rgba(255, 255, 255, 0.95);
            padding: 2.5rem;
            border-radius: 30px;
            box-shadow: 0 15px 35px rgba(219, 112, 147, 0.2);
            border: 2px solid #ffb6c1;
            max-width: 400px;
            width: 90%;
            z-index: 10;
        }

        /* Profile Image Styling */
        .profile-img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid #d63384;
            margin-bottom: 1rem;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        h1 { color: #d63384; font-size: 2rem; margin: 10px 0; }
        .subtext { font-size: 1.1rem; color: #7d4d5e; margin-bottom: 1.5rem; }

        #timer {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-bottom: 1.5rem;
        }

        .unit {
            background: #d63384;
            color: white;
            padding: 8px;
            border-radius: 12px;
            min-width: 60px;
        }

        .unit span { display: block; font-size: 1.5rem; font-weight: bold; }
        .label { font-size: 0.6rem; text-transform: uppercase; letter-spacing: 1px; }

        .btn {
            background: #d63384;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 50px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn:hover { background: #b02a6b; transform: scale(1.05); }

        .sparkle {
            position: absolute;
            background: #ff69b4;
            border-radius: 50%;
            opacity: 0.3;
            pointer-events: none;
            animation: float 6s infinite ease-in;
        }

        @keyframes float {
            0% { transform: translateY(100vh) scale(0); opacity: 0; }
            50% { opacity: 0.6; }
            100% { transform: translateY(-10vh) scale(1.2); opacity: 0; }
        }
    </style>
</head>
<body>

    <div class="card">
        <img src="shambhavi.jpg" alt="Shambhavi" class="profile-img" onerror="this.src='https://via.placeholder.com/150?text=Shambhavi'">
        
        <h1>For Shambhavi ✨</h1>
        <p class="subtext">Something magical is coming on Jan 6th...</p>
        
        <div id="timer">
            <div class="unit"><span id="days">00</span><div class="label">Days</div></div>
            <div class="unit"><span id="hours">00</span><div class="label">Hours</div></div>
            <div class="unit"><span id="mins">00</span><div class="label">Mins</div></div>
        </div>

        <button class="btn" onclick="revealHint()">Reveal a Hint</button>
        <p id="hint-text" style="margin-top: 20px; color: #d63384; font-weight: bold; display: none;"></p>
    </div>

    <script>
        const targetDate = new Date("Jan 6, 2026 00:00:00").getTime();

        function updateTimer() {
            const now = new Date().getTime();
            const diff = targetDate - now;
            const d = Math.floor(diff / (1000 * 60 * 60 * 24));
            const h = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const m = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            document.getElementById("days").innerText = d;
            document.getElementById("hours").innerText = h;
            document.getElementById("mins").innerText = m;
        }

        setInterval(updateTimer, 1000);
        updateTimer();

        function revealHint() {
            const hint = document.getElementById("hint-text");
            hint.innerText = "Hint: Wear your favorite outfit and keep your evening free! 👗✨";
            hint.style.display = "block";
        }

        for (let i = 0; i < 30; i++) {
            let div = document.createElement('div');
            div.className = 'sparkle';
            div.style.left = Math.random() * 100 + 'vw';
            div.style.width = div.style.height = Math.random() * 8 + 4 + 'px';
            div.style.animationDelay = Math.random() * 5 + 's';
            document.body.appendChild(div);
        }
    </script>
</body>
</html>
